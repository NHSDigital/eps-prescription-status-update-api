info:
  name: Apigee psu metadata
  type: http
  seq: 3

http:
  method: GET
  url: https://{{host}}/prescription-status-update/metadata
  headers:
    - name: x-request-id
      value: "{{$guid}}"
    - name: x-correlation-id
      value: "{{$guid}}"
  auth:
    type: bearer
    token: "{{authorization_header_value}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
