info:
  name: Apigee PULL REQUEST psu ping
  type: http
  seq: 1

http:
  method: GET
  url: https://{{host}}/prescription-status-update-pr-{{aws_pull_request_id}}/_ping
  headers:
    - name: x-request-id
      value: "{{$guid}}"
    - name: x-correlation-id
      value: "{{$guid}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
