info:
  name: Apigee PULL REQUEST psu metadata
  type: http
  seq: 1

http:
  method: GET
  url: https://{{host}}/prescription-status-update-pr-{{aws_pull_request_id}}/metadata
  headers:
    - name: x-request-id
      value: "{{$guid}}"
    - name: x-correlation-id
      value: "{{$guid}}"
  auth:
    type: bearer
    token: "{{authorization_header_value}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
