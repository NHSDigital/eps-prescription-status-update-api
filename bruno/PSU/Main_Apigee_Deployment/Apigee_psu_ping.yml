info:
  name: Apigee psu ping
  type: http
  seq: 3

http:
  method: GET
  url: https://{{host}}/prescription-status-update/_ping
  headers:
    - name: x-request-id
      value: "{{$guid}}"
    - name: x-correlation-id
      value: "{{$guid}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
