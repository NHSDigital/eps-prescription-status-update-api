{
  "name": "postDatedLambda",
  "version": "1.0.0",
  "description": "Lambda scheduled to process post-dated prescription notifications",
  "main": "main.js",
  "author": "NHS Digital",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "unit": "POWERTOOLS_DEV=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --coverage",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.30.0",
    "@aws-sdk/client-dynamodb": "^3.817.0",
    "@aws-sdk/client-sqs": "^3.817.0",
    "@aws-sdk/util-dynamodb": "^3.817.0",
    "@middy/core": "^6.4.5",
    "@middy/input-output-logger": "^6.4.5",
    "@nhs/fhir-middy-error-handler": "^2.1.65",
    "@psu-common/commonTypes": "^1.0.0"
  },
  "devDependencies": {
    "@psu-common/testing": "^1.0.0"
  }
}
