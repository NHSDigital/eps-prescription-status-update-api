type: object
properties:
  resourceType:
    type: string
    values: Bundle
  type:
    type: string
    values: transaction-response
    description: List of responses to status updates performed in the request transaction.
  entry:
    type: array
    items:
      type: object
      properties:
        response:
          type: object
          properties:
            status:
              type: string
              values: 201 Created
            outcome:
              type: object
              properties:
                resourceType:
                  type: string
                  values: OperationOutcome
                issue:
                  type: array
                  items:
                    type: object
                    properties:
                      severity:
                        type: string
                        values: information
                      code:
                        type: string
                        values: success
                      diagnostics:
                        type: string
                        values: No issues detected during validation
